/**
 * Shared module exports
 * AI-NOTE: Streamlined to only include genuinely reusable hooks
 * Components moved to /components/common, utils consolidated to /utils/formatters
 */

// Shared Hooks (only genuinely reusable ones)
export * from './hooks';